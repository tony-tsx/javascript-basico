const boolean1 = true

let resultado

if ( boolean1 && true )
  resultado = 'a condição foi verdadeira'
else resultado = 'a condição foi falsa'

const resultado2 = boolean1 && true
  ? 'a primeira condição foi verdadeira'
  : true
    ? 'a segunda condição foi verdadeira'
    : 'a primeira e a segunda condição foram falsas'
